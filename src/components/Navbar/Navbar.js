import './Navbar.css'

function Navbar() {
  return (
    <nav>
      <div>
        <a>LANDING PAGE</a>
        <a href="#">Login</a>
        <a href="#">Registro</a>
      </div>
    </nav>
  );
}

export default Navbar;
